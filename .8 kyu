def remove(st, n):
    removed_count = 0
    
    result = ""
    for char in st:
        if char == "!":
            if removed_count < n:
                removed_count += 1
            else:
                result += char
        else:
            result += char

    return result
    
    pass

